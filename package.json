{
  "name": "demoz-root",
  "private": true,
  "scripts": {
    "install": "cd tenant-backend && npm install",
    "build": "cd tenant-backend && npm run build",
    "security:audit": "npm audit --audit-level=high",
    "start": "cd tenant-backend && npm run start",
    "start:dev": "cd tenant-backend && npm run start:dev",
    "start:prod": "cd tenant-backend && npm run start:prod"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "@sentry/node": "^10.53.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "prisma-field-encryption": "^1.6.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
